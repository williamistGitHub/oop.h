#ifndef CBASE_H
#define CBASE_H

#include "oop.h"

DECL_CLASS_FUNCS(Base)
    // base class has no functions!
END_CLASS_FUNCS(Base)

DEF_CLASS(Base)
    int instVar;
END_CLASS_DEF(Base)

#endif//CBASE_H